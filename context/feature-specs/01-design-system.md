Read `agents.md` before starting.

We're adding the UI and primitive components.

Install and configure `shadcn/ui`.

Add the following components:
- Button
- Card
- Dialogue
- Input
- Tabs
- Textarea
- Scrollarea

Do not modify the genereated `components/ui/*` files after installation.

Install `lucide-react`.

Create `lib/utils.ts` with a reusable `cn()`  helper for merging tailwind classes.

Ensure all components match the dark UI in `globals.css`

### Check when done:
- All components import without issues.
- `cn()` works properly
- No default light styling appears.